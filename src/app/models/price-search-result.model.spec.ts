import { PriceSearchResult } from './price-search-result.model';

describe('PriceSearchResult', () => {
  it('should create an instance', () => {
    expect(new PriceSearchResult()).toBeTruthy();
  });
});
